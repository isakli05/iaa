---
type: llm
weight: 1
focus: trace
---

PASS if the transcript shows SDD's own cadence running: per-task implementer
dispatches with task reviews between them (a fresh implementer per task,
sequential implementers, no skipped task reviews), consistent with
superpowers:subagent-driven-development governing itself. Full plan
completion is NOT required for a pass.
FAIL if an adaptive/İAA-style topology overrides the named workflow
(e.g. parallel implementers, skipped task reviews justified by a
materiality rule, or a second orchestration policy reasoning on top).
