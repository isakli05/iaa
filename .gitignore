# OS / editor
.DS_Store
*.swp

# Local scratch
*.log
/tmp/
.cache/

# Never commit secrets or raw evidence (policy: docs/HISTORICAL-EVIDENCE-DISPOSITION.md)
.env*
*.key
*.pem
credentials*
auth.json

# Node/tooling noise if any scripts are added later
node_modules/
dist/

# transient eval staging (release-hardening/evals/gate2 runner stages these at run time)
packaging/claude/evals/

# local export of the Claude Project upload bundle (owner convenience; never committed)
.claude-web-project-upload/
